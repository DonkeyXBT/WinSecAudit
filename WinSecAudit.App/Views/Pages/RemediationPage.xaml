<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="WinSecAudit.Views.Pages.RemediationPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Remediation" Style="{StaticResource PageHeaderStyle}"/>
            <TextBlock Text="Apply security fixes and hardening recommendations." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,24,0,0">
            <StackPanel Spacing="16">
                <TextBlock Text="Available Remediations" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="SmbBtn" Click="Smb_Click">Harden SMB</Button>
                    <Button x:Name="LegacyBtn" Click="Legacy_Click">Disable Legacy Protocols</Button>
                    <Button x:Name="UacBtn" Click="Uac_Click">Strengthen UAC</Button>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <ScrollViewer>
                <TextBlock x:Name="OutputText" TextWrapping="Wrap" FontFamily="Consolas"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
