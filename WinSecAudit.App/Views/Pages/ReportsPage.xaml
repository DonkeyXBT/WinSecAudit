<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="WinSecAudit.Views.Pages.ReportsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Reports" Style="{StaticResource PageHeaderStyle}"/>
            <TextBlock Text="Export audit results in various formats." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,24,0,0">
            <StackPanel Spacing="16">
                <TextBlock Text="Export Format" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="HtmlBtn" Click="Html_Click">Export HTML</Button>
                    <Button x:Name="JsonBtn" Click="Json_Click">Export JSON</Button>
                    <Button x:Name="CsvBtn" Click="Csv_Click">Export CSV</Button>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="Recent Reports" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListView x:Name="ReportsList" SelectionMode="None"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
